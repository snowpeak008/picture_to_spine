---
doc_id: F2S-DOC-REQ-001
revision: 1.5
status: draft
canonical_for:
  - F2S-UC-001
  - F2S-UC-002
  - F2S-UC-003
  - F2S-UC-004
  - F2S-UC-005
  - F2S-UC-006
  - F2S-UC-007
  - F2S-UC-008
  - F2S-UC-009
  - F2S-UC-010
  - F2S-FR
  - F2S-NFR
  - F2S-AC
  - F2S-ENV-WIN-001
  - F2S-ENV-WIN-002
  - F2S-ENV-RGPU-001
  - F2S-ENV-SPINE-001
  - F2S-FIX-CHAR-001
  - F2S-FIX-POSE-001
  - F2S-FIX-PROJ-001
  - F2S-FIX-CORRUPT-001
  - F2S-FIX-SPINE-001
  - F2S-FIX-FAULT-001
  - F2S-FIP
depends_on:
  - F2S-DOC-GOV-001
  - F2S-DOC-CHARTER-001
review_score_ref: F2S-SCORE-DOC-REQ-001-R2
last_verified: 2026-07-11
---

# 需求规格与验收标准

## 1. 文档目的

本文档把 `F2S-DOC-CHARTER-001` 的产品承诺转化为可测试需求。它是功能范围、非功能属性和发布验收的权威来源。实现方案可以调整，但不得在未修改本文件并完成影响审查的情况下删除、弱化或重新解释 P0 需求。

关键字含义：

- **必须**：P0 发布门禁，不满足则首版不能完成；
- **应该**：P1 是声明范围内的商业发布要求；若延期，必须先通过需求变更删除相应功能/兼容声明，不能用 waiver 保留未兑现声明；
- **可以**：P2 增强项，不得阻塞 P0/P1；只有 P2 才可按 `F2S-REL-WAIVER-001` 在有期限、owner和证据时延期；
- **验证**：通过自动测试、人工操作、文件检查、网络观测或 Spine Editor 4.2.43 实测形成证据。

## 2. 验收前提与测试夹具

### 2.1 标准环境

| 环境 ID | 配置 | 用途 |
| --- | --- | --- |
| `F2S-ENV-WIN-001` | Windows 11 x64、16 GB RAM、8 核 CPU、RTX 3060 Ti 8 GB、100 GB 可用磁盘 | P0 一级平台、本地主验收与低显存验证 |
| `F2S-ENV-WIN-002` | Windows 10 22H2 x64、16 GB RAM、无可用 CUDA GPU | P1 尽力兼容、CPU/降级与兼容性观测，不作为 P0 发布阻断环境 |
| `F2S-ENV-RGPU-001` | 用户控制的私有 GPU 测试端点、TLS、可注入超时/断线 | 远程 Provider 验收 |
| `F2S-ENV-SPINE-001` | 合法安装的 Spine Editor Professional 4.2.43 | JSON/CLI 兼容实测 |

环境配置是发布前目标，实际硬件和软件版本必须记录在证据中；未执行时状态为 `not_run`，不得记为通过。

### 2.2 Golden 夹具

| 夹具 ID | 内容 |
| --- | --- |
| `F2S-FIX-CHAR-001` | 许可明确的原创/合成二次元类人侧视角色母版，2048×2048 RGBA，单 `primary_weapon` |
| `F2S-FIX-POSE-001` | 覆盖十个动作的关键姿势图和动作文字，不包含动作视频 |
| `F2S-FIX-PROJ-001` | 完成母版、分层、Rig、十个动作、事件和命中帧的基准项目 |
| `F2S-FIX-CORRUPT-001` | 截断清单、缺失 blob、错误校验和、旧 schema、保存中断等故障项目集合 |
| `F2S-FIX-SPINE-001` | 由导出器产生、供 Spine Editor 4.2.43 验证的确定性 JSON/atlas/PNG 集合 |
| `F2S-FIX-FAULT-001` | 固定30个持久化故障注入点及注入模式的manifest；hash冻结，任何失败点不得从KPI分母删除 |

由于用户不能提供公开测试素材，所有进入仓库和安装包的夹具必须由项目自制或采购，且许可证证据随夹具保存。

### 2.3 `F2S-FIX-FAULT-001` 固定故障点

`F2S-FIP` 命名空间由本文拥有。每一点都在标准fixture上执行表中模式；`process_kill` 表示持久化动作后立即强制终止，`disk_full`/`permission_denied` 在该写入前注入，`corrupt` 在提交后仅破坏所列可重建对象。预期统一为：只能恢复完整旧revision或完整新revision，审批与Job终态不伪造，隔离物可解释。M00冻结manifest字节和hash，后续只可增点，不能删除/改义旧点。

| Point ID | 精确边界 | 注入模式 |
| --- | --- | --- |
| `F2S-FIP-001` | validate完成、journal写入前 | process_kill |
| `F2S-FIP-002` | journal=`preparing`并flush后 | process_kill |
| `F2S-FIP-003` | Artifact temp首块写入后 | process_kill |
| `F2S-FIP-004` | Artifact temp完整flush后、CAS提升前 | process_kill |
| `F2S-FIP-005` | ApprovalRecord temp写入后 | process_kill |
| `F2S-FIP-006` | ApprovalRecord/AuditEvent block flush后 | process_kill |
| `F2S-FIP-007` | revision manifest temp写入后 | process_kill |
| `F2S-FIP-008` | revision manifest flush后 | process_kill |
| `F2S-FIP-009` | 必选CAS对象全部提升后 | process_kill |
| `F2S-FIP-010` | current pointer temp写入后 | process_kill |
| `F2S-FIP-011` | current pointer temp flush后 | process_kill |
| `F2S-FIP-012` | current pointer原子替换后 | process_kill |
| `F2S-FIP-013` | pointer目录flush/等价durability barrier后 | process_kill |
| `F2S-FIP-014` | journal=`committed`并flush后 | process_kill |
| `F2S-FIP-015` | commit完成、staging清理前 | process_kill |
| `F2S-FIP-016` | autosave manifest temp写入后 | process_kill |
| `F2S-FIP-017` | autosave manifest flush后 | process_kill |
| `F2S-FIP-018` | autosave pointer替换后 | process_kill |
| `F2S-FIP-019` | Project revision已提交、index apply前 | process_kill |
| `F2S-FIP-020` | Project revision已提交后破坏redb | corrupt |
| `F2S-FIP-021` | Job requested记录flush后 | process_kill |
| `F2S-FIP-022` | Job queued记录flush后 | process_kill |
| `F2S-FIP-023` | Worker进程创建后、running记录前 | process_kill |
| `F2S-FIP-024` | Job running记录flush后 | process_kill |
| `F2S-FIP-025` | Worker输出半行/半文件时 | process_kill |
| `F2S-FIP-026` | sandbox输出完整、验证前 | process_kill |
| `F2S-FIP-027` | 输出验证通过、CAS提升前 | process_kill |
| `F2S-FIP-028` | JobOutputArtifact提升后、succeeded记录前 | process_kill |
| `F2S-FIP-029` | succeeded记录后、RegisterCandidate前 | process_kill |
| `F2S-FIP-030` | candidate revision提交后、sandbox清理前 | process_kill |

除上述30个分母点外，`F2S-TST-111` 还对 `F2S-FIP-003`、`F2S-FIP-007`、`F2S-FIP-010` 注入 `disk_full`，对 `F2S-FIP-009`、`F2S-FIP-012` 注入 `permission_denied`；这些附加模式不能替代30个必跑point。

## 3. 用例

| 用例 ID | 主体 | 成功结果 |
| --- | --- | --- |
| `F2S-UC-001` | 主操作者 | 创建/打开项目，保存、恢复并查看当前门禁状态 |
| `F2S-UC-002` | 主操作者 | 导入角色图，检查透明度和构图，批准角色母版 |
| `F2S-UC-003` | 动作设计审核者 | 填写风格、角色、武器和动作描述，获得可复制的 AI 动作提示词包与素材计划 |
| `F2S-UC-004` | 主操作者 | 自动建议并手工修正分层、隐藏区域和重组结果，批准图层 |
| `F2S-UC-005` | 动画师/技术美术 | 创建和调整骨骼、槽位、附件、网格、权重及约束，批准 Rig |
| `F2S-UC-006` | 动画师 | 为十个动作编辑时间轴、关键帧、曲线和事件，并运行诊断 |
| `F2S-UC-007` | 动作设计审核者 | 比较关键姿势，确认攻击接触、命中帧和恢复阶段 |
| `F2S-UC-008` | 技术美术 | 输出交付物，并在用户本地 Spine Editor 4.2.43 验证 |
| `F2S-UC-009` | 主操作者 | 从崩溃、断电模拟、取消或外部任务失败中恢复 |
| `F2S-UC-010` | 技术美术 | 显式配置私有远程 GPU，检查发送范围，运行、取消和恢复任务 |

## 4. 功能需求

### 4.1 项目与工作区

| 需求 ID | 优先级 | 需求 |
| --- | --- | --- |
| `F2S-FR-PROJ-001` | P0 | 系统必须创建目录型项目，并在创建前验证目标路径可写、剩余空间和路径规范。 |
| `F2S-FR-PROJ-002` | P0 | 系统必须打开、关闭、另存和列出最近项目；不存在的最近项必须可清理且不阻塞启动。 |
| `F2S-FR-PROJ-003` | P0 | 系统必须执行事务化保存：新版本完整写入并通过校验后才能替换项目当前指针。 |
| `F2S-FR-PROJ-004` | P0 | 系统必须自动保存可恢复草稿，并区分“已保存”“未保存”“自动恢复”“下游已失效”。 |
| `F2S-FR-PROJ-005` | P0 | 系统必须记录不可变操作审计，包括审批、撤销审批、外部任务、导出和恢复。 |
| `F2S-FR-PROJ-006` | P0 | 系统必须在 schema 不兼容时先备份、迁移或以只读方式打开，不得原地破坏未知版本。 |
| `F2S-FR-PROJ-007` | P1 | 系统应该生成脱敏诊断包；默认排除原图、派生图、提示词正文和项目机密。 |

### 4.2 图片输入与母版

| 需求 ID | 优先级 | 需求 |
| --- | --- | --- |
| `F2S-FR-IMP-001` | P0 | 系统必须支持拖放和文件选择导入 PNG、JPEG、WebP；V1 不接受 PSD 输入，选择 PSD 时必须无副作用拒绝并说明可先导出为受支持图片。 |
| `F2S-FR-IMP-002` | P0 | 导入必须验证格式签名、尺寸、色彩空间、alpha、文件大小、解码错误和重复内容，不得只信任扩展名。 |
| `F2S-FR-IMP-003` | P0 | 原始输入必须按内容哈希只读保存；所有裁剪、去背、分割和补全创建派生版本。 |
| `F2S-FR-IMP-004` | P0 | 用户必须能设置角色朝向、基准地面、像素比例、画布安全边界和 `primary_weapon` 是否已包含。 |
| `F2S-FR-MASTER-001` | P0 | 用户必须能够预览母版、放大检查、比较规范化前后结果，并显式批准或拒绝。 |
| `F2S-FR-MASTER-002` | P0 | 母版变化必须使所有依赖的分层、Rig、动作与导出标记为 `stale`，但保留旧版本和映射报告。 |

### 4.3 规格、素材规划与提示词

| 需求 ID | 优先级 | 需求 |
| --- | --- | --- |
| `F2S-FR-SPEC-001` | P0 | 系统必须提供 CharacterSpec、StyleSpec、WeaponSpec、RuntimeSpec、MotionSpec 和 AssetSpec 的结构化编辑与 schema 验证。 |
| `F2S-FR-SPEC-002` | P0 | 十个基线动作必须全部存在，动作 ID 和语义遵循 `F2S-DOC-CONTENT-001`；不得静默删除。 |
| `F2S-FR-PLAN-001` | P0 | 系统必须按动作判定 bone/mesh/attachment/sequence/new-view/vfx 策略，并允许用户覆盖且记录原因。 |
| `F2S-FR-PLAN-002` | P0 | AssetSpec 每项必须包含来源、用途动作、视角、父骨建议、pivot、重叠、状态和审批记录。 |
| `F2S-FR-PLAN-003` | P0 | 系统必须聚合重复素材需求，显示覆盖动作和未使用素材，并在试跑诊断后创建补图任务。 |
| `F2S-FR-PROMPT-001` | P0 | 系统必须根据已批准规格生成版本化 AI 动作提示词包，包含正向、负向、关键姿势、构图、透明背景和一致性约束。 |
| `F2S-FR-PROMPT-002` | P0 | 提示词包必须可单项复制、整体导出和重新导入关联；系统不得自行调用第三方生图服务。 |
| `F2S-FR-PROMPT-003` | P0 | 外部生成图片重新导入时，用户必须选择对应 PromptItem/AssetSpec；无法匹配时进入待归类区。外部工具/模型信息无法取得时，系统必须写入 `unknown_external` 并保存用户来源声明，不得伪造模型、版本或 seed。 |

### 4.4 分层与图像修正

| 需求 ID | 优先级 | 需求 |
| --- | --- | --- |
| `F2S-FR-LAYER-001` | P0 | 系统必须提供自动分层建议和全手工路径；自动能力不可用时不得阻断手工生产。 |
| `F2S-FR-LAYER-002` | P0 | 蒙版编辑必须支持画笔、橡皮、套索、填充、羽化受控、收缩/扩张、撤销/重做和独显。 |
| `F2S-FR-LAYER-003` | P0 | 图层必须可命名、分组、排序、指定身体语义、可见性、锁定、父骨建议和前后遮挡。 |
| `F2S-FR-LAYER-004` | P0 | 隐藏区域补全必须保留原可见像素，新增像素以可视覆盖层标记。本地或私有远程的内部 AI 任务必须记录 Provider、算法/模型及版本、参数、seed（若适用）、遮罩、源版本、输入/输出哈希和任务 ID；缺少这些 provenance 时结果不得进入候选。 |
| `F2S-FR-LAYER-005` | P0 | 系统必须提供分层重组与批准母版的差异视图，包括透明洞、边缘色污染、重叠不足和未覆盖像素诊断。 |
| `F2S-FR-LAYER-006` | P0 | 用户必须逐层接受、修正或替换结果，并对整套分层显式批准。 |
| `F2S-FR-LAYER-007` | P1 | 系统应该支持从外部编辑器重新载入单层，并在尺寸或基准变化时要求用户解决映射。 |

### 4.5 Rig、网格与约束

| 需求 ID | 优先级 | 需求 |
| --- | --- | --- |
| `F2S-FR-RIG-001` | P0 | 系统必须提供可编辑骨骼树，支持创建、删除、重命名、重父级、平移、旋转、缩放和骨长调整。 |
| `F2S-FR-RIG-002` | P0 | 槽位和附件必须有稳定 ID，支持父骨、draw order、blend/颜色、默认附件和动作专用附件。 |
| `F2S-FR-RIG-003` | P0 | 用户必须能设置和可视化 pivot、武器握持点、地面点、脚接触点及其他 socket。 |
| `F2S-FR-RIG-004` | P0 | 系统必须支持网格顶点/三角形/边界编辑、权重绘制、归一化、镜像受控和影响骨限制。 |
| `F2S-FR-RIG-005` | P0 | 系统必须实现 Rig IR 支持范围内的 IK、transform 和 path 约束，未支持 Spine 特性必须在导出前报告。 |
| `F2S-FR-RIG-006` | P0 | 临时 Rig 必须能在素材尚未完整时用占位附件试跑动作，并把失败关联回 AssetSpec。 |
| `F2S-FR-RIG-007` | P0 | Rig 诊断必须至少检查循环父级、孤儿附件、零尺寸骨、无效三角形、未归一权重、过多影响骨和 draw-order 冲突。 |
| `F2S-FR-RIG-008` | P0 | 用户必须显式批准 Rig；结构变化应使依赖动作和导出按影响范围失效。 |

### 4.6 动画与游戏性审核

| 需求 ID | 优先级 | 需求 |
| --- | --- | --- |
| `F2S-FR-ANIM-001` | P0 | 系统必须为十个动作提供独立时间轴，支持骨骼、槽位、附件、draw order、deform、constraint、event 和 sequence 所需轨道。 |
| `F2S-FR-ANIM-002` | P0 | 时间轴必须支持添加、移动、复制、删除关键帧，区间缩放，多选，吸附，阶梯/线性/贝塞尔曲线和可撤销编辑。 |
| `F2S-FR-ANIM-003` | P0 | 系统必须支持循环区间、播放速度、逐帧、洋葱皮、关键姿势标记和参考图叠加。 |
| `F2S-FR-ANIM-004` | P0 | 系统必须支持动作描述和关键姿势图作为参考，但不得把参考图自动认定为准确骨骼或玩法数据。 |
| `F2S-FR-ANIM-005` | P0 | 系统必须运行透明洞、网格拉伸、轮廓跳变、脚滑、武器脱手、地面穿透、draw order 和循环接缝诊断。 |
| `F2S-FR-GAME-001` | P0 | `attack_01`、`attack_02`、`attack_03` 必须分别可标记 anticipation、active/hit、recovery、weapon_contact 和事件；`hit`、`death` 必须可标记关键阶段。 |
| `F2S-FR-GAME-002` | P0 | 命中帧、关键姿势和武器接触必须由用户显式批准；AI 或模板只能提出候选。 |
| `F2S-FR-GAME-003` | P0 | 用户修改动作帧率、时长或关键帧后，关联的事件与命中审批必须按规则失效并提示重新确认。 |
| `F2S-FR-ANIM-006` | P1 | 系统应该允许在动作间复制可兼容轨道，并在骨架或附件不一致时提供冲突报告。 |

### 4.7 内部预览、比较与报告

| 需求 ID | 优先级 | 需求 |
| --- | --- | --- |
| `F2S-FR-PREV-001` | P0 | 内部渲染器必须直接读取 Rig IR，不依赖 Spine Runtime 或 Spine Editor。 |
| `F2S-FR-PREV-002` | P0 | 预览必须支持透明/纯色/棋盘/用户背景、像素倍率、画布裁剪、骨骼/网格/slot/pivot/命中标记叠加。 |
| `F2S-FR-PREV-003` | P0 | 系统必须支持批准版本与工作版本的同步播放、差异热图或帧对帧比较。 |
| `F2S-FR-PREV-004` | P0 | 所有诊断必须标明规则、严重度、类别、关联需求、证据帧、对象、建议动作、`waiverPolicy` 和实际处置；禁止接受的项目不得出现风险接受结果。 |
| `F2S-FR-REPORT-001` | P0 | 系统必须生成动作覆盖、素材覆盖、审批状态、诊断、导出兼容和许可证来源报告。 |

### 4.8 导出与 Spine 验证

| 需求 ID | 优先级 | 需求 |
| --- | --- | --- |
| `F2S-FR-EXP-001` | P0 | 系统必须导出版本化、带 schema 的 Rig IR，并能重新导入得到等价语义。 |
| `F2S-FR-EXP-002` | P0 | 系统必须导出透明 PNG 层和最小可重建分层 PSD，至少保留画布、像素层、组/层级、名称、顺序、可见性、alpha、origin/pivot 映射及 manifest，且不覆盖源图。Adjustment、智能对象、图层样式等高级 PSD 特性允许明确报告为降级或不支持，但不得把缺少 P0 最小信息的文件标为成功。 |
| `F2S-FR-EXP-003` | P0 | 系统必须导出目标为 Spine Editor 4.2.43 的 JSON 和 atlas 输入资料；名称、路径、坐标、颜色和时间单位必须确定性。 |
| `F2S-FR-EXP-004` | P0 | 导出前必须运行 preflight；错误阻止导出，警告需要用户确认并写入报告。 |
| `F2S-FR-EXP-005` | P0 | 系统必须检测用户本地 Spine Professional CLI，显示版本和授权由用户负责的声明，并只通过受控参数调用。 |
| `F2S-FR-EXP-006` | P0 | CLI 不存在、版本不是 4.2.43 或验证失败时仍可保存导出文件，但状态必须为 `exported_unverified` 或 `incompatible`。 |
| `F2S-FR-EXP-007` | P0 | Spine 验证必须保存命令模板的脱敏形式、目标版本、退出码、日志摘要、产物哈希和时间。 |
| `F2S-FR-EXP-008` | P0 | AI 提示词包、规格快照、AssetSpec 清单和验证报告必须作为正式交付物可单独导出。 |

### 4.9 本地/私有远程计算

| 需求 ID | 优先级 | 需求 |
| --- | --- | --- |
| `F2S-FR-COMP-001` | P0 | 核心手工编辑、项目管理、内部预览和导出必须在没有 AI 模型、没有 GPU、没有网络时可用。 |
| `F2S-FR-COMP-002` | P0 | 本地任务必须在执行前估算 CPU、RAM、VRAM、临时磁盘并提供低显存/分块/CPU 选项。 |
| `F2S-FR-COMP-003` | P0 | 长任务必须显示阶段、进度、资源、日志摘要，支持取消，并在安全点产生可恢复 checkpoint。 |
| `F2S-FR-RGPU-001` | P0 | 私有远程 GPU Provider 必须默认关闭，并要求用户显式输入端点、TLS/证书信任、认证和允许的数据类型。 |
| `F2S-FR-RGPU-002` | P0 | 发送前必须显示将上传的源/派生对象及大小；用户批准必须与任务记录绑定。 |
| `F2S-FR-RGPU-003` | P0 | 客户端必须验证返回对象与请求、哈希和 schema；异常结果隔离，不得直接覆盖已批准资产。 |
| `F2S-FR-RGPU-004` | P0 | 超时、断线、服务重启和取消必须产生确定状态，可恢复或安全重试，且不得自动切换到第三方云。 |

### 4.10 应用启动与交付

| 需求 ID | 优先级 | 需求 |
| --- | --- | --- |
| `F2S-FR-APP-001` | P0 | 工程主目录必须提供一个用户可双击的项目入口文件；开发态入口负责环境检查和启动，发布态入口启动安装后的桌面应用。 |
| `F2S-FR-APP-002` | P0 | 双击入口失败时必须显示可读错误和日志位置，不得出现一闪而过且无诊断的终端窗口。 |
| `F2S-FR-APP-003` | P0 | 安装包必须支持标准用户安装或明确请求管理员权限，卸载不得删除用户项目。 |
| `F2S-FR-APP-004` | P1 | 应用应该支持 `.f2sproj` 或等价入口关联，但关联文件不得包含项目全部资产。 |

## 5. 非功能需求

### 5.1 性能与容量

| 需求 ID | 优先级 | 可测要求 |
| --- | --- | --- |
| `F2S-NFR-PERF-001` | P0 | 在 `F2S-ENV-WIN-001` 上，打开 `F2S-FIX-PROJ-001` 到可交互状态的 P95 不超过 10 秒，不含首次模型加载。 |
| `F2S-NFR-PERF-002` | P0 | 画布平移、缩放、骨骼拖动和时间轴拖动的输入到可见反馈 P95 不超过 100 ms。 |
| `F2S-NFR-PERF-003` | P0 | 标准夹具在 1920×1080 预览窗口、骨骼叠加关闭时持续达到 60 FPS；诊断叠加时不低于 30 FPS。 |
| `F2S-NFR-PERF-004` | P0 | 10 分钟以上任务不得阻塞 UI 消息循环超过 200 ms，进度在活动阶段至少每 2 秒刷新。 |
| `F2S-NFR-PERF-005` | P1 | 工程应定义并显示纹理、附件、顶点、关键帧和缓存预算，超过软限发出建议，超过硬限拒绝不安全操作。 |
| `F2S-NFR-PERF-006` | P0 | 在 8 GB GPU 档启动本地 AI 任务前，系统必须预检模型、输入、VRAM/RAM 和临时磁盘预算；不足时在执行前阻止或由用户显式选择 tile/offload/CPU/较小模型，并记录质量影响，禁止静默降分辨率或降质。 |
| `F2S-NFR-PERF-007` | P0 | 对标准 fixture 的 manifest/revision 提交 P95 不超过 500 ms，不能阻塞画布；大型 artifact 的哈希、复制和内容寻址写入异步执行，并在原子切换前保持旧 revision 可用。 |

AI 推理耗时不设单一绝对承诺，因为模型、设备和远程端点可变；必须报告真实吞吐、阶段和峰值资源。

### 5.2 可靠性与恢复

| 需求 ID | 优先级 | 可测要求 |
| --- | --- | --- |
| `F2S-NFR-REL-001` | P0 | 任意可撤销编辑均不得使最后一个已确认项目版本不可打开。 |
| `F2S-NFR-REL-002` | P0 | 在保存关键阶段注入进程终止后，重启必须恢复旧确认版本或完整新版本，不得得到半写版本。 |
| `F2S-NFR-REL-003` | P0 | 撤销/重做以命令边界工作，连续 500 个标准编辑后状态与重放结果一致。 |
| `F2S-NFR-REL-004` | P0 | 相同输入、相同 Rig IR 和相同导出配置必须产生语义相同且除允许时间字段外字节稳定的导出。 |
| `F2S-NFR-REL-005` | P0 | 应用崩溃后，恢复界面必须列出最后保存、自动保存和未完成任务，用户决定采用哪一个。 |

### 5.3 隐私与安全

| 需求 ID | 优先级 | 可测要求 |
| --- | --- | --- |
| `F2S-NFR-PRIV-001` | P0 | 私有远程 GPU 未启用时，离线测试和网络代理观测不得发现项目数据、提示词或遥测外发。 |
| `F2S-NFR-PRIV-002` | P0 | 日志默认不得包含图像字节、完整提示词、认证令牌、用户目录清单或可还原的隐私内容。 |
| `F2S-NFR-SEC-001` | P0 | 所有文件输入、IPC 和 Provider 返回均进行 schema、大小、路径和内容验证；canonical export只能落在项目根，用户明确选择的项目外根仅允许在项目内提交成功后接收publish copy，临时文件必须位于该外部根同卷；另只允许`F2S-DOC-STORE-001`定义且由核心创建的授权Job sandbox root。任何相对/链接解析都不得逃逸对应根。 |
| `F2S-NFR-SEC-002` | P0 | 密钥使用 Windows Credential Manager 或等价 OS 安全存储，不得明文写入项目或日志。 |
| `F2S-NFR-SEC-003` | P0 | 外部 CLI 调用使用参数数组和允许列表，不经 shell 拼接用户输入；超时、退出码和输出大小受限。 |
| `F2S-NFR-SEC-004` | P0 | 远程端点必须使用 TLS 并通过主机名、有效期、链和用户固定指纹策略；证书错误不得忽略。发布二进制不存在跳过开关；隔离测试只能把专用 localhost 证书显式加入临时测试信任库，不能关闭验证。 |
| `F2S-NFR-SEC-005` | P0 | 商业包中任何处理 D2 项目数据的本地 Worker 必须使用唯一 `windows-appcontainer-v1` SandboxProfile：restricted AppContainer token、无 network capability、专用 ACL Job root、Job Object 子进程/资源约束及 OS egress 恶意探针全部通过；任一控制不可用时必须禁用 Provider，Worker Pack 不得进入发布物。 |

### 5.4 易用性、可访问性与国际化

| 需求 ID | 优先级 | 可测要求 |
| --- | --- | --- |
| `F2S-NFR-UX-001` | P0 | 每个审批门必须显示输入、检查结果、下游影响和批准/退回动作；禁用按钮要给出原因。 |
| `F2S-NFR-UX-002` | P0 | 所有破坏性动作必须说明影响范围；可恢复操作优先进入版本/废纸篓，不直接永久删除。 |
| `F2S-NFR-A11Y-001` | P0 | 非画布核心流程可仅用键盘完成；焦点可见，文本与关键控件满足 WCAG 2.2 AA 对比度。 |
| `F2S-NFR-A11Y-002` | P0 | 状态不得只依赖颜色；错误、警告、AI 派生像素和审批均有形状、图标或文本标识。 |
| `F2S-NFR-A11Y-003` | P0 | UI token 自动测试必须覆盖 app/surface/elevated/canvas/input 及 hover/focus/selected/disabled/error/warning/stale 组合；普通和 disabled 文本均执行至少 4.5:1 的项目硬门，非文本控件边界和焦点至少 3:1，并在 100%/125%/150%/200% DPI 做截图抽查。 |
| `F2S-NFR-I18N-001` | P0 | UI 首发为简体中文，数据 ID、文件 schema 和 Spine 名称使用稳定英文标识；文本资源不得硬编码在业务逻辑。 |
| `F2S-NFR-DPI-001` | P0 | 100%、125%、150%、200% Windows 缩放下，主要页面无不可达控件、模糊文字或错误画布坐标。 |

### 5.5 兼容、维护与许可

| 需求 ID | 优先级 | 可测要求 |
| --- | --- | --- |
| `F2S-NFR-COMPAT-001` | P0 | Windows 11 x64 是一级支持与发布阻断平台；发布矩阵必须记录精确系统构建。 |
| `F2S-NFR-COMPAT-002` | P1 | Windows 10 22H2 x64 提供尽力兼容；必须记录已测构建、通过/失败项和已知限制，但失败不冒充 P0 通过，也不自动阻断 Windows 11 发布。 |
| `F2S-NFR-SPINE-001` | P0 | 仅对 Spine Editor 4.2.43 宣称验证；其他版本显示未经验证，不自动改写目标版本。 |
| `F2S-NFR-MAINT-001` | P0 | 项目、Rig IR、PromptPack、Provider 和导出格式均有独立 schemaVersion 与迁移测试。 |
| `F2S-NFR-OBS-001` | P0 | 日志使用结构化事件 ID、相关任务 ID 和严重度；用户可查看但隐私字段默认脱敏。 |
| `F2S-NFR-LIC-001` | P0 | 发布物的依赖、模型、字体、图标和样例资产均有 SBOM、来源、版本、哈希和许可证结论。 |
| `F2S-NFR-LIC-002` | P0 | `F2S-LIC-POLICY-001` allowlist外或未知的发布组件必须阻塞构建；策略变更需ADR与法务证据，不能在依赖级静默放行。 |

## 6. 核心验收场景

### 6.1 项目与母版

**`F2S-AC-PROJ-001`**

- 给定空目录和 `F2S-FIX-CHAR-001`；
- 当用户分别以 PNG、JPEG、WebP 创建项目、导入、设置侧视/地面/比例并批准母版，并尝试导入一个 PSD；
- 则三种 P0 图片格式能够进入不可变原图流程，PSD 被无副作用拒绝且给出转换说明；
- 则原图按哈希保存，工作版本与原图分离，审批审计存在，项目重开后状态一致。

**`F2S-AC-PROJ-002`**

- 给定保存中断故障注入；
- 当应用在写清单、写 blob 和切换当前指针三个阶段分别终止；
- 则重启只能恢复完整旧版本或完整新版本，并说明恢复来源。

### 6.2 规格、提示词和素材闭环

**`F2S-AC-CONTENT-001`**

- 给定动作文字和 `F2S-FIX-POSE-001`；
- 当用户完成六类 Spec 并生成内容；
- 则十个动作都有稳定 ID、阶段、表现策略、最小素材、提示词项和未决问题，且无孤儿 AssetSpec。

**`F2S-AC-CONTENT-002`**

- 给定 `attack_02` 的临时 Rig 在挥击中出现肘部透明洞和前臂强透视；
- 当诊断完成；
- 则系统分别创建隐藏区域修正和透视附件任务，关联证据帧，不通过简单加大 mesh 拉伸静默处理。

**`F2S-AC-PROMPT-001`**

- 给定已批准 StyleSpec 和母版；
- 当生成并导出 PromptPack；
- 则包内含母版引用、不可改变项、每个关键姿势正/负向提示、输出规格和 AssetSpec 映射，且未产生任何外部网络请求；外部回导无法提供生成元数据时保存 `unknown_external` 和用户声明。

### 6.3 分层与 Rig

**`F2S-AC-LAYER-001`**

- 给定自动分层服务不可用；
- 当用户选择手工路径；
- 则用户仍可完成蒙版、层级、隐藏区域外部导入、重组差异和图层批准。

**`F2S-AC-LAYER-002`**

- 给定母版与批准分层；
- 当执行重组检查；
- 则母版可见区域的未授权像素变化、透明洞和重叠不足均可定位到层和像素区域。

**`F2S-AC-RIG-001`**

- 给定批准分层；
- 当用户建立骨架、槽位、网格、权重、握持点和约束并批准；
- 则所有附件有稳定父级与 pivot，诊断无阻塞错误，保存重开后结构和数值一致。

### 6.4 动画与玩法审核

**`F2S-AC-ANIM-001`**

- 给定批准 Rig；
- 当用户完成十个动作；
- 则每个动作可播放、逐帧、循环或结束，诊断结果可追踪；`attack_01`、`attack_02`、`attack_03` 分别具有 anticipation/active/recovery 和武器接触。

**`F2S-AC-GAME-001`**

- 给定 AI 或模板建议的命中帧；
- 当用户未点击批准；
- 则动作和项目不得进入 `gameplay_approved` 或 `export_ready`。

**`F2S-AC-GAME-002`**

- 给定已经批准的攻击命中帧；
- 当用户移动相关关键帧或改变 FPS；
- 则审批变为 `stale`，旧值保留供比较，导出门提示重新审核。

### 6.5 导出与 Spine

**`F2S-AC-EXP-001`**

- 给定 `F2S-FIX-PROJ-001`；
- 当连续两次以相同配置导出；
- 则 Rig IR、PNG、满足 P0 最小可重建契约的 PSD、Spine JSON/atlas 输入、PromptPack 和报告先作为完整canonical snapshot原子提交到项目内`exports/<export-id>`，确定性字段一致；高级 PSD 降级逐项记录且不会掩盖最小契约失败。若用户选择项目外目录，系统随后以该外部根内同卷staging→校验→rename发布可重建副本；publish失败保留内部成功snapshot并显示独立失败，不出现半成品或把外部目录当真值。

**`F2S-AC-EXP-002`**

- 给定合法 Spine Editor 4.2.43；
- 当运行可选 CLI 验证；
- 则目标版本、命令摘要、退出码、日志和产物哈希被记录，Golden 项目通过约定结构与动作检查。

**`F2S-AC-EXP-003`**

- 给定 Spine 未安装、版本不符或 CLI 失败；
- 当用户请求导出；
- 则文件仍可保存，但界面和报告均不能显示 `spine_verified`。

### 6.6 隐私、远程与启动

**`F2S-AC-PRIV-001`**

- 给定全新安装且未配置私有端点；
- 当完成端到端流程并通过网络代理观测；
- 则没有项目数据、提示词、遥测或崩溃内容外发。

**`F2S-AC-RGPU-001`**

- 给定受信任私有端点；
- 当用户查看数据清单、批准发送并在任务中途断开网络；
- 则任务进入可解释状态，批准资产不被覆盖，可重试或取消，且不存在公共云回退。

**`F2S-AC-APP-001`**

- 给定干净 Windows 验收机；
- 当用户双击工程主目录入口；
- 则应用可启动或显示明确缺失依赖与日志位置；发布安装后可新建、保存、关闭并重开项目。

## 7. 需求失效与变更规则

| 上游变化 | 必须失效的下游 |
| --- | --- |
| 母版像素、朝向、比例或地面变化 | 分层比较、Rig 映射、全部动作预览、导出 |
| 图层边界、尺寸、pivot 或语义变化 | 关联网格、权重、附件轨道、动作诊断、导出 |
| 骨骼层级或 rest pose 变化 | 关联约束、权重、全部动画和游戏性审批 |
| MotionSpec 时长/FPS/阶段变化 | 动画诊断、关键姿势、事件与命中帧审批 |
| WeaponSpec 握持点或尺寸变化 | 手型、武器附件、attack 动作接触和命中审批 |
| 导出目标或 Spine patch 变化 | 全部兼容验证；新 patch 视为新的适配任务 |

实现必须通过依赖图计算失效范围，禁止用单个布尔 `dirty` 掩盖影响原因。

## 8. 需求到目标追踪

| 目标 | 主要用例 | 核心需求域 |
| --- | --- | --- |
| `F2S-GOAL-001` | `F2S-UC-001`、`F2S-UC-009`、`F2S-UC-010` | PROJ、COMP、RGPU、PRIV、SEC |
| `F2S-GOAL-002` | `F2S-UC-003`、`F2S-UC-004`、`F2S-UC-006` | SPEC、PLAN、PROMPT、LAYER、ANIM |
| `F2S-GOAL-003` | `F2S-UC-002`、`F2S-UC-003`、`F2S-UC-004`、`F2S-UC-005`、`F2S-UC-006`、`F2S-UC-007` | MASTER、LAYER、RIG、GAME、PREV |
| `F2S-GOAL-004` | `F2S-UC-008` | EXP、SPINE、LIC、APP |

`F2S-DOC-TRACE-001` 已逐项展开每一个 FR/NFR 的 AC、精确测试、reserved DEV 和目标门禁；原子开发计划必须沿用这些 ID 并补真实 evidence path。只实现页面或函数、不形成可执行验收链的任务不算完成。

## 9. 发布阻断条件

以下任意一项存在时，发布判定失败：

- P0 需求没有测试或证据；
- 母版、分层、Rig、关键姿势、命中帧任一门禁可被后台任务静默越过；
- 禁止联网模式仍存在未说明的外发；
- 项目源资产仅存在于数据库或缓存；
- AI 派生像素无法追踪到源和遮罩；
- 提示词功能调用了第三方生图 API；
- Spine JSON 未在 4.2.43 实测却显示已验证；
- 发布包包含白名单外且未获批准的代码、权重、字体、图标或样例资产；
- 双击入口无法启动且没有可读诊断；
- 保存或迁移故障可能破坏最后一个确认版本；
- 十个动作中任一缺少规格、预览或必要审批。
