# 2026-07-12-002

## 摘要

本轮围绕实际试用暴露的“单图手绘遮罩分层过难”问题，确认当前分层主流程需要重设计：把 PSD / 透明 PNG LayerPack 资源包作为主输入路径，把现有手绘遮罩降级为高级修补。已完成 LayerPack 设计文档、可执行原子任务卡，并将清理后的项目源码推送到 GitHub `snowpeak008/picture_to_spine`。

## 完成内容

- [x] 指导用户完成母版 StyleSpec 填写、母版预览和审批，确认当前工具不是单图直接生成序列帧动画，而是 Spine 生产候选流程。
- [x] 分析分层工作台现状，确认 `空遮罩层 16` 的根因是 17 个必需语义层中只有 1 个层有像素，手绘遮罩不适合作为普通用户主流程。
- [x] 讨论并裁定新输入方向：PSD / 透明 PNG 分层资源包为主路径，允许受控重叠，用 draw order、pivot、socket、合成预览和人工审批替代严格无重叠像素分配。
- [x] 编写设计文档 `plan/NEWpic_reload/01-PSD分层资源包输入重设计.md`，覆盖文件夹命名、三视图命名、manifest、Rig profile、QA、无 PS 用户路径、LayerPack Studio 和实现路线。
- [x] 编写顺序计划 `plan/NEWpic_reload/dev/00-PSD分层资源包原子开发计划.md`。
- [x] 按用户反馈补充真正面向开发执行的任务卡 `plan/NEWpic_reload/dev/01-原子任务卡-可执行版.md`，共 31 张 NRD 原子任务卡，每张包含开发内容、设计范围、实现标准、验收标准、测试要求、交付物和回滚方式。
- [x] 初始化本地 Git 仓库，清理 `target/`、`node_modules/`、`dist/`、根目录 exe、tsbuildinfo 和 zip 归档，新增 `.gitignore` / `.gitattributes`。
- [x] 将当前纯净项目提交并 force-with-lease 推送到 `https://github.com/snowpeak008/picture_to_spine`，远程 `main` 为提交 `1995b5d Initial clean FlashToSpine project import`。

## 验证

- [x] 读取并确认新增设计文档可正常显示。
- [x] 读取并确认 `plan/NEWpic_reload/dev/00-PSD分层资源包原子开发计划.md` 存在。
- [x] 读取并确认 `plan/NEWpic_reload/dev/01-原子任务卡-可执行版.md` 存在，包含 31 张 `NRD-*` 任务卡。
- [x] Git 推送后确认 `origin/main` 指向 `1995b5d610b51d2b289cd0afcdfece3d5a5d3d91`，本地 `main...origin/main` 干净。
- [ ] 未运行构建/测试，因为按用户要求清理了依赖与构建产物；后续执行需先重新安装依赖。

## 后续关注

- [ ] 若继续实现，优先从 `NRD-001` 到 `NRD-019` 做最小可用 LayerPack 导入闭环。
- [ ] 不要把 Photoshop、Krita、GIMP、Clip Studio、模型权重或公网图片服务作为内置生产依赖；无 PS 用户路径优先实现 LayerPack Studio。
- [ ] LayerPack 新 QA 要允许受控重叠，旧手绘遮罩只保留为 repair mode。
- [ ] 下次改动源码后需要运行项目要求的格式、类型、测试和集成检查，再更新 evidence。
