import type { ImportCandidate } from './importStore';
export function PreflightPanel({candidate}:{candidate:ImportCandidate}){return <div className="preflight-panel"><header><b>Rust 权威预检</b><em>{candidate.status}</em></header><dl><div><dt>格式</dt><dd>{candidate.mediaType}</dd></div><div><dt>尺寸</dt><dd>{candidate.width} × {candidate.height}</dd></div><div><dt>位深</dt><dd>{candidate.bitDepth}-bit</dd></div><div><dt>文件大小</dt><dd>{(candidate.byteLength/1024/1024).toFixed(2)} MiB</dd></div><div><dt>完整解码</dt><dd className="good">{candidate.completeDecode?'已通过':'失败'}</dd></div><div><dt>SHA-256</dt><dd title={candidate.sourceSha256}>{candidate.sourceSha256.slice(0,12)}…</dd></div><div><dt>审批状态</dt><dd>未批准</dd></div></dl><p>同一组字节已经在独立子进程中执行 magic、8-bit、资源预算、完整解码和稳定哈希；提升 CAS 后仍只是候选。</p></div>}
