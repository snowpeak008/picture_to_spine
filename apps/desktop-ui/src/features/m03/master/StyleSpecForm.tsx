import type { StyleSpecDraft } from '../M03Gateway';

export function StyleSpecForm({ value, onChange, disabled = false }: { value: StyleSpecDraft; onChange: (value: StyleSpecDraft) => void; disabled?: boolean }) {
  const weapon = value.primaryWeapon;
  const enable = () => onChange({ ...value, primaryWeapon: { weaponType: '', gripMode: 'one-hand', weaponHand: 'far-hand', socketSemantic: 'weapon-grip', sizeClass: 'medium', silhouetteConstraints: '' } });
  return <fieldset className="style-spec" disabled={disabled}><legend>StyleSpec</legend><label>表现风格<input value={value.renderingStyle} onChange={(event) => onChange({ ...value, renderingStyle: event.target.value })} /></label><label>轮廓语言<input value={value.outline} onChange={(event) => onChange({ ...value, outline: event.target.value })} /></label><label>配色备注<textarea value={value.paletteNotes} onChange={(event) => onChange({ ...value, paletteNotes: event.target.value })} /></label><label>身份锚点<textarea value={value.identityNotes} onChange={(event) => onChange({ ...value, identityNotes: event.target.value })} /></label><div className="locked-spec"><span>视角</span><b>side-view</b><span>主武器数量</span><b>固定 1</b></div>{weapon ? <><label>武器类型<input value={weapon.weaponType} onChange={(event) => onChange({ ...value, primaryWeapon: { ...weapon, weaponType: event.target.value } })} /></label><label>握持方式<select value={weapon.gripMode} onChange={(event) => onChange({ ...value, primaryWeapon: { ...weapon, gripMode: event.target.value as typeof weapon.gripMode } })}><option value="one-hand">单手</option><option value="two-hand">双手</option><option value="flexible">可切换</option></select></label><label>持武器手<select value={weapon.weaponHand} onChange={(event) => onChange({ ...value, primaryWeapon: { ...weapon, weaponHand: event.target.value as typeof weapon.weaponHand } })}><option value="near-hand">近侧手</option><option value="far-hand">远侧手</option><option value="both-hands">双手</option></select></label><label>尺寸级别<select value={weapon.sizeClass} onChange={(event) => onChange({ ...value, primaryWeapon: { ...weapon, sizeClass: event.target.value as typeof weapon.sizeClass } })}><option value="small">小型</option><option value="medium">中型</option><option value="large">大型</option></select></label><label>武器 Socket<input value={weapon.socketSemantic} onChange={(event) => onChange({ ...value, primaryWeapon: { ...weapon, socketSemantic: event.target.value } })} /></label><label>侧视轮廓约束<textarea value={weapon.silhouetteConstraints} onChange={(event) => onChange({ ...value, primaryWeapon: { ...weapon, silhouetteConstraints: event.target.value } })} /></label></> : <button type="button" className="secondary" onClick={enable}>确认并填写主武器语义</button>}</fieldset>;
}
